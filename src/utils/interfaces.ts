export type Person = {
    name: string,
    surname: string,
}

export type Setup = {
    stack: string[],
    link: string,
}

export type Contribution = {
    icon: string,
    title: string,
    message: string,
    link: string,
}
